# Security policy

Never commit `DEEPDECK_API_KEY`, `MTG_ENGINE_API_KEY`, a session cookie, or a private
release-signing key. Use environment variables and revoke an exposed token immediately.

Report vulnerabilities through a private GitHub security advisory, not a public issue.
