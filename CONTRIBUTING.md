# Contributing

Public forks and pull requests are welcome. Only `@dd-the-dd` may merge or push to the
protected `main` branch.

The Alexios policy must remain readable and generic over card rules. Prefer a reusable
classification such as `changeTargets`, `goad`, or `destroyPermanent` over a branch for
one printing. Every strategy change needs a focused test.

